; Single-node mode: CouchDB will create system databases (_users, _replicator, _global_changes)
; Required when not using the cluster setup wizard (e.g. Docker single container).
[couchdb]
single_node = true
